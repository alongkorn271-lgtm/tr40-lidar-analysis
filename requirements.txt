# Runtime dependencies for the TR40 LiDAR Analysis Suite
#
# Install with:   pip install -r requirements.txt
# Python:         3.10 or newer recommended

numpy>=1.24
pandas>=2.0
openpyxl>=3.1          # Excel I/O backend used by pandas
matplotlib>=3.7
customtkinter>=5.2     # modern Tk theme used by main_v2.py (primary GUI)

# tkinter ships with the Python standard library — no pip install needed.
